# ALL IN ONE VISION

A version-based learning path: from absolute zero to CV-for-Robotics.

Each version lives in its own folder, is self-contained, and stays fully
runnable on its own — even though later versions will replace or extend it.

## Version Log

| Version | Folder                  | Goal                                   | Status  |
|---------|--------------------------|-----------------------------------------|---------|
| v1      | `v1_mnist_classifier/`   | Classic MNIST digit classification, MLP baseline | ✅  |
| v2+     | (not created yet)        | CNN upgrade, then real-world image data, then robotics-specific CV | ✅ |

Future versions are
intentionally NOT scaffolded yet — per the "build only what's needed now"
principle. 

This file will be updated as each new version is proposed and
approved.
